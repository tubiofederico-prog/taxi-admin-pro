'use client';
import {drivers} from '@/lib/mock-data/drivers';
import {useParams} from 'next/navigation';
import Link from 'next/link';
export default function DriverDetail(){const p=useParams();const d=drivers.find(dr=>dr.id===parseInt(p.id as string));if(!d)return(<div className="text-center py-12"><p className="text-gray-600">No encontrado</p><Link href="/drivers" className="text-purple-600">Volver</Link></div>);return(<div className="space-y-6"><Link href="/drivers" className="text-purple-600 text-sm font-medium">← Volver</Link><div className="flex justify-between items-start"><div><h1 className="text-3xl font-bold">{d.name}</h1><p className="text-gray-600">{d.email} • {d.phone}</p></div><span className={`text-sm px-4 py-2 rounded-full ${d.status==='aprobado'?'bg-green-100 text-green-700':'bg-yellow-100 text-yellow-700'}`}>{d.status}</span></div><div className="grid grid-cols-4 gap-4">{[{l:'Viajes',v:d.totalTrips},{l:'Rating',v:d.rating>0?`⭐${d.rating}`:'-'},{l:'Ingresos',v:`S/ ${d.earnings}`},{l:'Docs',v:d.documentStatus}].map(i=>(<div key={i.l} className="bg-white p-6 rounded-lg border"><p className="text-gray-600 text-sm">{i.l}</p><h2 className="text-2xl font-bold mt-2">{i.v}</h2></div>))}</div></div>);}
