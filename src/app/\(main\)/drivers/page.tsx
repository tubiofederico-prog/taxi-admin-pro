'use client';
import {drivers} from '@/lib/mock-data/drivers';
import Link from 'next/link';
import {useState} from 'react';
export default function DriversPage(){const [s,setS]=useState('');const f=drivers.filter(d=>d.name.toLowerCase().includes(s.toLowerCase())||d.email.toLowerCase().includes(s.toLowerCase()));return(<div className="space-y-6"><div><h1 className="text-3xl font-bold text-gray-900">Gestión de Conductores</h1><p className="text-gray-600 mt-1">Administra y aprueba conductores</p></div><input type="text" placeholder="Buscar..." value={s} onChange={(e)=>setS(e.target.value)} className="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 w-full max-w-xs"/><div className="bg-white rounded-lg border border-gray-200 overflow-hidden"><table className="w-full"><thead className="bg-gray-50"><tr><th className="px-6 py-3 text-left text-sm font-semibold">Nombre</th><th className="px-6 py-3 text-left text-sm font-semibold">Email</th><th className="px-6 py-3 text-left text-sm font-semibold">Estado</th><th className="px-6 py-3 text-left text-sm font-semibold">Acciones</th></tr></thead><tbody>{f.map(d=>(<tr key={d.id} className="border-b hover:bg-gray-50"><td className="px-6 py-4 font-medium">{d.name}</td><td className="px-6 py-4">{d.email}</td><td className="px-6 py-4"><span className={`text-xs font-medium px-3 py-1 rounded-full ${d.status==='aprobado'?'bg-green-100 text-green-700':d.status==='pendiente'?'bg-yellow-100 text-yellow-700':'bg-red-100 text-red-700'}`}>{d.status}</span></td><td className="px-6 py-4"><Link href={`/drivers/${d.id}`} className="text-purple-600 font-medium">Ver</Link></td></tr>))}</tbody></table></div></div>);}
